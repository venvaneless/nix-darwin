# CODEX: CLAUDE-MEM WHAT-THE
# =========================
# Explain technical topics in plain language without persistent configuration

{ ... }:

{
  # NO CONFIGURATION
  # =========================
  # What-the is a prompt-only skill. It reads the current conversation and
  # writes no persistent settings or runtime files.
}
