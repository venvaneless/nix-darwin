# CODEX: CLAUDE-MEM MEMORY SEARCH
# =========================
# Query the shared Claude-mem database without separate skill configuration

{ ... }:

{
  # NO CONFIGURATION
  # =========================
  # Memory search uses the worker and database selected by shared Claude-mem
  # settings. It does not create a skill-specific config file.
}
