- You once said:

```
"What people split out is the outputs part, and you already do that. flake-parts pulls in ./flake-modules/*.nix, and haumea and den organise the modules below that. The inputs block is still a single list in every one of those setups."
```

My nix-config in general should be heavily modularised using flake-parts, haumea and Den

What does dentristic mean?
