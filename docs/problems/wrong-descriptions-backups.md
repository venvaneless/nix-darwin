This is the wrong way of writing the description:

```
# Values only. Every knob below is declared in
# options/backups/app-backup-helper.nix, which owns what each one means
# and how the backup is carried out.
```

It should describe what kind of tool, package, etc. the backup is for and short description what the action will be
