# shared/terminal/cli-tuis/micro/themes/default.nix
#
# =====================================================================
# MICRO: AVAILABLE THEMES
#
# Registry of Micro themes available for selection in ../micro.nix.
# =====================================================================

{
  atom-dark = {
    module = ./atom-dark.nix;
    themeName = "atom-dark";
  };

  bubblegum = {
    module = ./bubblegum.nix;
    themeName = "bubblegum";
  };

  catppuccin = {
    module = ./catppuccin-mocha.nix;
    themeName = "catppuccin-mocha";
  };

  cmc-16 = {
    module = ./cmc-16.nix;
    themeName = "cmc-16";
  };

  cmc-tc = {
    module = ./cmc-tc.nix;
    themeName = "cmc-tc";
  };

  darcula = {
    module = ./darcula.nix;
    themeName = "darcula";
  };

  default = {
    module = ./default-theme.nix;
    themeName = "default";
  };

  dracula-tc = {
    module = ./dracula-tc.nix;
    themeName = "dracula-tc";
  };

  dukedark-tc = {
    module = ./dukedark-tc.nix;
    themeName = "dukedark-tc";
  };

  dukelight-tc = {
    module = ./dukelight-tc.nix;
    themeName = "dukelight-tc";
  };

  dukeubuntu-tc = {
    module = ./dukeubuntu-tc.nix;
    themeName = "dukeubuntu-tc";
  };

  geany = {
    module = ./geany.nix;
    themeName = "geany";
  };

  gotham = {
    module = ./gotham.nix;
    themeName = "gotham";
  };

  gruvbox = {
    module = ./gruvbox.nix;
    themeName = "gruvbox";
  };

  gruvbox-light = {
    module = ./gruvbox-light.nix;
    themeName = "gruvbox-light";
  };

  gruvbox-tc = {
    module = ./gruvbox-tc.nix;
    themeName = "gruvbox-tc";
  };

  material-tc = {
    module = ./material-tc.nix;
    themeName = "material-tc";
  };

  monokai = {
    module = ./monokai.nix;
    themeName = "monokai";
  };

  monokai-dark = {
    module = ./monokai-dark.nix;
    themeName = "monokai-dark";
  };

  one-dark = {
    module = ./one-dark.nix;
    themeName = "one-dark";
  };

  railscast = {
    module = ./railscast.nix;
    themeName = "railscast";
  };

  simple = {
    module = ./simple.nix;
    themeName = "simple";
  };

  solarized = {
    module = ./solarized.nix;
    themeName = "solarized";
  };

  solarized-tc = {
    module = ./solarized-tc.nix;
    themeName = "solarized-tc";
  };

  sunny-day = {
    module = ./sunny-day.nix;
    themeName = "sunny-day";
  };

  test-scheme = {
    module = ./test-scheme.nix;
    themeName = "test-scheme";
  };

  twilight = {
    module = ./twilight.nix;
    themeName = "twilight";
  };

  zenburn = {
    module = ./zenburn.nix;
    themeName = "zenburn";
  };
}
